import { IsInActiveCategoryPipe } from './is-in-active-category.pipe';

describe('IsInActiveCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInActiveCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
